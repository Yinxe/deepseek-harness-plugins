# @dshp/token-meter bundle patch: unified TokenMeter for DeepSeek Harness.
# Host half: multi-vendor quota snapshots + session-log stats engine + /ext/dshp-token-meter/* routes.
# Client half: sidebar-footer quota button (ring + popover) + Settings page section + stats widgets.

- insert:
    - id: dshp-token-meter
      name: '@dshp/token-meter'
