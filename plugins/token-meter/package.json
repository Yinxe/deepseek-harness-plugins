{
  "name": "@dshp/token-meter",
  "version": "0.13.0",
  "description": "DeepSeek Harness (DSH) TokenMeter: unified quota + usage statistics — multi-vendor rolling/payg quota with sidebar card and provider ops, plus session-log token aggregation with trends, heatmap and per-model breakdown. 额度与用量统计统一视图：多供应商 rolling/payg 额度（侧边栏卡片 + 供应商运维操作），外加会话日志 token 聚合（趋势、热力图、按模型拆分）。",
  "license": "MIT",
  "author": "Yinxe",
  "type": "module",
  "main": "./lib/host.js",
  "exports": {
    ".": "./lib/host.js",
    "./client": "./lib/client.js",
    "./package.json": "./package.json"
  },
  "files": [
    "lib",
    "cordis.patch.yml",
    "README.md"
  ],
  "keywords": [
    "dsh",
    "deepseek-harness",
    "cordis",
    "plugin",
    "token",
    "quota",
    "billing",
    "usage",
    "statistics"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsup --watch",
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf lib *.tsbuildinfo",
    "test": "node --check lib/host.js && node --check lib/client.js && node ../../shared/scripts/check-css-modules.mjs src/client && node scripts/check-share-tokens.mjs && node scripts/check-quota-templates.mjs && node scripts/check-widget-bridge.mjs && node scripts/check-widget-kit-bridge.mjs && echo \"syntax + token coverage + quota templates + widget bridge ok\"",
    "prepublishOnly": "pnpm typecheck && pnpm build",
    "lint": "oxlint --deny-warnings --config ../../.oxlintrc.json src tsup.config.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Yinxe/deepseek-harness-plugins.git",
    "directory": "plugins/token-meter"
  },
  "bugs": {
    "url": "https://github.com/Yinxe/deepseek-harness-plugins/issues"
  },
  "homepage": "https://github.com/Yinxe/deepseek-harness-plugins/tree/main/plugins/token-meter#readme",
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web"
    }
  },
  "peerDependencies": {
    "@deepseek-ai/dsh-client-ui-primitives": "^0.1.5-rc.1"
  },
  "devDependencies": {
    "@deepseek-ai/dsh-client-ui-primitives": "0.1.5-rc.1",
    "@dshp/widget-kit": "workspace:*",
    "@deepseek-ai/schemastery": "^3.18.2",
    "@types/react": "~18.3.1",
    "@types/react-dom": "~18.3.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tsup": "^8.5.1"
  },
  "dependencies": {}
}
