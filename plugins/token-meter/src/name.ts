/**
 * 插件的**展示名**（唯一来源）。
 *
 * 用在哪：中心区 tab 标签、设置分区标签、分享面板页头、额度查询空状态、以及各处
 * 「去 设置 → X 里改」的引导文案 —— host 半的报错建议也用它（例如「设置 → X → 该供应商 → 编辑」）。
 *
 * 为什么单独放一个常量：这个名字出现在 client 与 host 两个 bundle、十几处文案里，
 * 散着写必然出现"改了 tab 忘了报错文案"这种漏改。运行时也不接受别名 —— 包名（`@dshp/token-meter`）
 * 是标识符，永远不改；这里改的只是**给人看的名字**。
 */
export const DISPLAY_NAME = 'Token 总览';
